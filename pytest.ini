[pytest]
pythonpath = .
addopts = -m "not llm and not slow"
markers =
    unit: fast unit tests with no network or real LLM calls
    integration: integration tests using local services/data
    e2e: end-to-end tests
    llm: tests that may call a real LLM provider
    slow: tests expected to take longer than ordinary unit tests
